import { Multiple } from "./model.multiple"
import { Training } from "./model.training"
import { User } from "./model.user"
import { throwError } from "./utils"

export class TrainingService {

	// addTraining(user: User, training: Training) {}

	// getTraining(user: User, trainingId: string): Training | null {return null;}

}


// interface MultipleData {
// 	f1: number,
// 	f2: number,
// 	successes: number,
// 	fails: number,
// }

// function toMultipleData(multiple: Multiple): MultipleData {
// 	const {f1, f2, successes, fails} = multiple;
// 	return {f1, f2, successes, fails};
// }
